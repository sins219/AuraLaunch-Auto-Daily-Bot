RPC_URL=https://liteforge.rpc.caldera.xyz/infra-partner-http
DRY_RUN=false

# Stake amount: a token number (e.g. 1) or a balance percentage (e.g. 50%)
STAKE_AURA=1
STAKE_REV=1

# Auto-loop interval (ms). Default 8 hours (28800000) when unset.
DAEMON_INTERVAL_MS=28800000
